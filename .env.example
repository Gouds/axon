# Role: brain | controller | desktop (default: desktop)
PLATFORM_ROLE=desktop

# Force all hardware drivers to mock mode (auto-set in desktop role)
MOCK_HARDWARE=true

# Brain URL — used by controller when not overridden by active profile
# BRAIN_URL=http://192.168.4.44:8000
